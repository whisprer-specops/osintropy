# Core runtime deps (no heavy ML)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dotenv>=1.0.0
tqdm>=4.65.0

# Browser automation (only if you actually use the selenium-based collectors)
selenium==4.15.0
undetected-chromedriver==3.5.3
fake-useragent==1.4.0

# Fuzzy matching / enrichment
fuzzywuzzy==0.18.0
python-Levenshtein==0.21.1
phonenumbers==8.13.25
python-dateutil==2.8.2

# Export (lightweight)
openpyxl>=3.1.0

# Rate limiting
ratelimit>=2.2.1
